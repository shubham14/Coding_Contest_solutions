#include <iostream>
#include <stdio.h>
#include <vector>
#define COL 10

using namespace std;

int minHealth(int A[][COL], int m, int n)
{
    int ans = 0;
    ans += A[0][0]
    for(int i = 0; i < m; i++)
    {
        for(int j = 0; j < n; j++)
        {
            if(i==0 && j==0)
                continue;
            else
            {
                ans
            }
        }
    }
    return 0;
}

int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
